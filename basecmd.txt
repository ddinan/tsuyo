exports.run = async (client, message, args, level) => {
  const prefix = message.guild === null ? ';;' : client.getSettings(message.guild.id).prefix
  try {
  //command code
  
  //error log
  
  } catch (err) {
    where.send('There was an error!\n' + err).catch();
  }
};

//permissions and aliases

exports.conf = {
  enabled: true,
  aliases: [],
  guildOnly: false,
  permLevel: 'User'
};


//what the command does

exports.help = {
  name: '',
  category: '',
  description: '',
  usage: ''
};
